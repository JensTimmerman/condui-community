/**
 * Font family string for Konva/canvas text. Matches the `@font-face` family.
 *
 * Hard-locked to Figtree (no font switching).
 */
export function useCanvasFontFamily(): string {
  return 'Figtree'
}
