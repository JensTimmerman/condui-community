export const COLLAPSED_PANEL_SIGNIFIER_WIDTH_PX = 36
export const VIEWPORT_RATIO_MIN = 0.05
export const VIEWPORT_RATIO_MAX = 0.95

/** Above editor top bar (`z-[220]`); below header dropdowns (`z-[230]`). */
export const FLOATING_DOCKABLE_PANEL_Z_INDEX = 225

/** Matches `3.5rem` top chrome offset used in `Layout` header menus. */
export const EDITOR_TOP_BAR_HEIGHT_PX = 56

/** Above editor top bar (`z-[220]`) and header dropdowns (`z-[230]`). */
export const PLAN_DROP_CIRCUIT_PANEL_Z_INDEX = 235
